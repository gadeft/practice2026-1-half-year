# Завдання 2. 
> Реалізувати простий REST-сервер на Flask або FastAPI з ендпоінтами
/users (GET — отримання списку, POST — створення) та /users/<id> (GET, PUT,
DELETE). Дані зберігати у списку або JSON-файлі. Забезпечити правильну структуру
відповіді, що містить поля status, data, message.


## <div style="color: red">**The AI doesn't work, because the resources were exhausted. So it is just echo bot😢**</div>
