# Завдання 6. 
> Створити Dockerfile для простої програми (наприклад, Flask-сервер або
скрипт). Зібрати образ і запустити контейнер. Додати файл docker-compose.yml, який
піднімає застосунок і базу даних (наприклад, PostgreSQL). Перевірити взаємодію
контейнерів.

#### **The AI doesn't work, because the resources were exhausted. So it is just echo bot😢**
