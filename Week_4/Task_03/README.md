# Завдання 3. 
> Створити програму, яка використовує змінні оточення для конфігурації
(наприклад, DB_URL, API_KEY). Зберегти ці параметри у файлі .env і підключити їх
через відповідну бібліотеку. Пояснити, чому не можна зберігати паролі
безпосередньо у коді.

> Паролі не можна зберігати в коді, тому що при передачі цього 
> коду іншим особам (наприклад комусь для огляду чи цей код був
> "запушений" на GitHub) їм буде видно всі паролі.

#### **The AI doesn't work, because the resources were exhausted. So it is just echo bot😢**
