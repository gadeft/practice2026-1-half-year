# Завдання 5. 
> Створити REST-API з авторизацією користувачів, у якому при вході
генерується JWT-токен. Реалізувати ендпоінт /profile, який потребує валідного токена
для доступу. Зберігати паролі у вигляді гешів із використанням bcrypt. Показати, що
без токена доступ заборонений.

#### **The AI doesn't work, because the resources were exhausted. So it is just echo bot😢**
